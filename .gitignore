# Vault (personal notes — never commit)
data/vault/
data/triage*

# Google Takeout exports (personal data)
data/export/
data/tasks-dump.json

# OAuth tokens (credentials — never commit)
data/.gmail-access-token.json
data/.google-tokens.json
data/.calendar-tokens.json

# Recommendations store (personal data — never commit)
data/recommendations.db

# Cost + activity tracking DBs (live on VPS, never commit)
workspace/cost-tracker.db
workspace/activity-log.db

# Generated status export (rebuilt by export-status.py)
workspace/jimbo-status.json

# Email digest data (contains email content — never commit)
data/email-digest.json
data/email-backlog-*.json
data/feedback-*.json
data/sample-maildir/
data/sift-cron*.log
data/sift-perf.log
data/.sift-seen.json

# Firebase
firebase-debug.log

# Python
__pycache__/
*.pyc

# Astro blog build artifacts
workspace/blog-src/node_modules/
workspace/blog-src/dist/
workspace/blog-src/.astro/

# Claude Code worktrees
.claude/worktrees/

# OS
.DS_Store
